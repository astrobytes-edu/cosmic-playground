---
export type TagPillProps = {
  label: string;
  tone?: "default" | "teal" | "violet" | "blue" | "magenta";
};

const { label, tone = "default" } = Astro.props as TagPillProps;
---

<span class="cp-badge" data-tone={tone === "default" ? undefined : tone}>
  {label}
</span>
