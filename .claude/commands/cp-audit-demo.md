---
description: Adversarial read-only audit of one Cosmic Playground demo
argument-hint: "<demo-slug>"
---

Use the `cosmic-adversarial-review` skill on the demo `$ARGUMENTS`.

Either do the review yourself or delegate it to the `adversarial-reviewer` agent (never more than one
reviewer agent at a time). Cover the exhibit, station and instructor pages as well as `/play/$ARGUMENTS/`.
Read-only: do not edit, commit or push. Report findings ordered by harm to a student, each marked
verified or lead, then what was not checked.
