{
  "name": "cosmic-playground",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@9.0.0",
  "type": "module",
  "scripts": {
    "dev": "corepack pnpm -C apps/site dev",
    "build": "node scripts/build.mjs",
    "assets:katex": "node scripts/copy-katex-assets.mjs",
    "lint:invariants": "node scripts/validate-invariants.mjs",
    "lint": "node scripts/validate-invariants.mjs && node scripts/validate-math-formatting.mjs && node scripts/validate-datasets.mjs && node scripts/validate-physics-models.mjs && node scripts/validate-plot-contract.mjs",
    "typecheck": "corepack pnpm -r typecheck",
    "test:invariants": "vitest run scripts/validate-invariants.test.mjs",
    "test:play-dirs": "vitest run scripts/validate-play-dirs.test.mjs",
    "test:datasets": "vitest run scripts/validate-datasets.test.mjs",
    "test:physics-contract": "vitest run scripts/validate-physics-models.test.mjs",
    "test:plot-contract": "vitest run scripts/validate-plot-contract.test.mjs",
    "test": "corepack pnpm -C packages/physics test && corepack pnpm -C packages/theme test && corepack pnpm -C packages/runtime test && corepack pnpm -C apps/demos test && corepack pnpm test:invariants && corepack pnpm test:play-dirs && corepack pnpm test:datasets && corepack pnpm test:physics-contract && corepack pnpm test:plot-contract"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
